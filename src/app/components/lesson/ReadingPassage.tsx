const ReadingPassage = () => (
  // <div className="md:col-span-2 bg-white/80 p-8 rounded-2xl border border-black/5">
  //   <h3 className="text-xl font-bold text-[#3E3636] mb-4">{item.title}</h3>
  //   <p className="text-lg leading-loose tracking-wide bg-[#F5EDED]/80 p-6 rounded-lg mb-8">{item.passage}</p>
  //   <div className="space-y-6">
  //     {item.questions.map((q, index) => (
  //       <div key={index}>
  //         <p className="font-semibold mb-3">{index + 1}. {q.question}</p>
  //         <div className="flex flex-col sm:flex-row sm:space-x-4 space-y-2 sm:space-y-0">
  //           {q.options.map(opt => (
  //             <button key={opt} className="flex-1 text-left p-3 bg-white rounded-md border hover:border-[#D72323] transition-colors">{opt}</button>
  //           ))}
  //         </div>
  //       </div>
  //     ))}
  //   </div>
  // </div>
  <></>
);

export default ReadingPassage;